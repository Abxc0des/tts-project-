/**
 * Site configuration — update after deploying to Netlify.
 * Replace SITE_URL with your live URL everywhere it appears.
 */
window.PDF_VOICE_CONFIG = {
  siteUrl: 'https://pdfvoice.netlify.app',

  /** Set after Google AdSense approval, e.g. 'ca-pub-1234567890123456' */
  adsenseClient: '',

  /** Optional: separate ad slot IDs from AdSense dashboard */
  adSlots: {
    controls: '',
    faq: '',
  },
};
